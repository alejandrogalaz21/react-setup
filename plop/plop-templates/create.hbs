import React from 'react'
import { connect } from 'react-redux'
import { {{camelCase name}}Actions } from './{{camelCase name}}.redux'
import {{pascalCase name}}Form from './{{pascalCase name}}Form'
import Banner from 'layouts/Site/Layouts/Banner'

function {{pascalCase name}}(props) {
  const submit = values => {
    props.create{{pascalCase name}}(values)
  }

  return (
    <Banner>
      < {{pascalCase name}} Form {...props} label='Agregar' button='add' onSubmit={submit} />
    </Banner>
  )
}

const mapStateToProps = ({ {{camelCase name}} }) => ({ {{camelCase name}} })
const mapDispatchToProps = {
  create{{pascalCase name}}: {{camelCase name}}Actions.create{{pascalCase name}}Request
}

export default connect(mapStateToProps, mapDispatchToProps)({{pascalCase name}}Create)